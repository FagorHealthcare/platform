# GENERATED by platform/observability/render.py — do not edit.

kind: Dashboard
metadata:
  name: pre-business
  project: fagor
spec:
  display:
    name: pre-business
  duration: 1h
  panels:
    intake_confirmation_rate:
      kind: Panel
      spec:
        display:
          name: Tomas confirmadas (count/h)
        plugin:
          kind: TimeSeriesChart
          spec: {}
        queries:
        - kind: TimeSeriesQuery
          spec:
            plugin:
              kind: PrometheusTimeSeriesQuery
              spec:
                datasource:
                  kind: PrometheusDatasource
                  name: loki-pre
                query: |-
                  sum(count_over_time({cluster="pre", container="md-core"}
                                      |~ "TOMA CONFIRMADA" [1h]))
                seriesNameFormat: confirmed
    intake_missed_rate:
      kind: Panel
      spec:
        display:
          name: Tomas olvidadas (count/h)
        plugin:
          kind: TimeSeriesChart
          spec: {}
        queries:
        - kind: TimeSeriesQuery
          spec:
            plugin:
              kind: PrometheusTimeSeriesQuery
              spec:
                datasource:
                  kind: PrometheusDatasource
                  name: loki-pre
                query: |-
                  sum(count_over_time({cluster="pre", container="md-core"}
                                      |~ "TOMA OLVIDADA" [1h]))
                seriesNameFormat: missed
    whatsapp_outbound_rate:
      kind: Panel
      spec:
        display:
          name: Mensajes WhatsApp salientes (count/h)
        plugin:
          kind: TimeSeriesChart
          spec: {}
        queries:
        - kind: TimeSeriesQuery
          spec:
            plugin:
              kind: PrometheusTimeSeriesQuery
              spec:
                datasource:
                  kind: PrometheusDatasource
                  name: loki-pre
                query: |-
                  sum(count_over_time({cluster="pre", container="md-core"}
                                      |~ "FROM:.*TO:.*MSG:" [1h]))
                seriesNameFormat: outbound
    daily_reports_requested:
      kind: Panel
      spec:
        display:
          name: Informes diarios solicitados (count/h)
        plugin:
          kind: TimeSeriesChart
          spec: {}
        queries:
        - kind: TimeSeriesQuery
          spec:
            plugin:
              kind: PrometheusTimeSeriesQuery
              spec:
                datasource:
                  kind: PrometheusDatasource
                  name: loki-pre
                query: |-
                  sum(count_over_time({cluster="pre", container="md-core"}
                                      |~ "Solicitado informe" [1h]))
                seriesNameFormat: requested
    patient_onboarding_events:
      kind: Panel
      spec:
        display:
          name: Eventos de onboarding de paciente (count/d)
        plugin:
          kind: TimeSeriesChart
          spec: {}
        queries:
        - kind: TimeSeriesQuery
          spec:
            plugin:
              kind: PrometheusTimeSeriesQuery
              spec:
                datasource:
                  kind: PrometheusDatasource
                  name: loki-pre
                query: |-
                  sum(count_over_time({cluster="pre", container="md-core"}
                                      |~ "Creating activation token|DISPOSITIVO .* ASIGNADO A PACIENTE" [1d]))
                seriesNameFormat: onboarding
  layouts:
  - kind: Grid
    spec:
      display:
        title: Main
      items:
      - x: 0
        y: 0
        width: 12
        height: 8
        content:
          $ref: '#/spec/panels/intake_confirmation_rate'
      - x: 12
        y: 0
        width: 12
        height: 8
        content:
          $ref: '#/spec/panels/intake_missed_rate'
      - x: 0
        y: 8
        width: 12
        height: 8
        content:
          $ref: '#/spec/panels/whatsapp_outbound_rate'
      - x: 12
        y: 8
        width: 12
        height: 8
        content:
          $ref: '#/spec/panels/daily_reports_requested'
      - x: 0
        y: 16
        width: 12
        height: 8
        content:
          $ref: '#/spec/panels/patient_onboarding_events'
