# GENERATED by platform/observability/render.py — do not edit.

kind: Dashboard
metadata:
  name: pre-overview
  project: fagor
spec:
  display:
    name: pre-overview
  duration: 1h
  panels:
    actualizacion_timeout_rate:
      kind: Panel
      spec:
        display:
          name: 504s en /vN/actualizacion (req/s)
        plugin:
          kind: TimeSeriesChart
          spec:
            thresholds:
              steps:
              - value: 0.02
                color: yellow
              - value: 0.1
                color: red
        queries:
        - kind: TimeSeriesQuery
          spec:
            plugin:
              kind: PrometheusTimeSeriesQuery
              spec:
                datasource:
                  kind: PrometheusDatasource
                  name: loki-pre
                query: |-
                  sum by (cluster) (
                    rate({cluster="pre", container="controller"}
                         |~ "actualizacion"
                         | json
                         | status="504" [5m])
                  )
                seriesNameFormat: '{{cluster}}'
    ingress_status_rate:
      kind: Panel
      spec:
        display:
          name: Status del ingress por código (req/s)
        plugin:
          kind: TimeSeriesChart
          spec: {}
        queries:
        - kind: TimeSeriesQuery
          spec:
            plugin:
              kind: PrometheusTimeSeriesQuery
              spec:
                datasource:
                  kind: PrometheusDatasource
                  name: loki-pre
                query: |-
                  sum by (status) (
                    rate({cluster="pre", container="controller"}
                         | json
                         | __error__="" [5m])
                  )
                seriesNameFormat: '{{status}}'
    actualizacion_failure_ratio:
      kind: Panel
      spec:
        display:
          name: Ratio de fallos /vN/actualizacion (504 ÷ total)
        plugin:
          kind: TimeSeriesChart
          spec:
            thresholds:
              steps:
              - value: 0.01
                color: yellow
              - value: 0.1
                color: red
        queries:
        - kind: TimeSeriesQuery
          spec:
            plugin:
              kind: PrometheusTimeSeriesQuery
              spec:
                datasource:
                  kind: PrometheusDatasource
                  name: loki-pre
                query: |-
                  sum(rate({cluster="pre", container="controller"}
                           |~ "actualizacion" | json | status="504" [5m]))
                  /
                  sum(rate({cluster="pre", container="controller"}
                           |~ "actualizacion" [5m]))
                seriesNameFormat: ratio 504/total
    md_core_quartz_error_rate:
      kind: Panel
      spec:
        display:
          name: md-core Quartz infra errors (req/s)
        plugin:
          kind: TimeSeriesChart
          spec:
            thresholds:
              steps:
              - value: 0.1
                color: yellow
              - value: 0.5
                color: red
        queries:
        - kind: TimeSeriesQuery
          spec:
            plugin:
              kind: PrometheusTimeSeriesQuery
              spec:
                datasource:
                  kind: PrometheusDatasource
                  name: loki-pre
                query: |-
                  sum(rate({cluster="pre", container="md-core"}
                           |~ "JobStoreCMT|ClusterManager|qrtz_|MisfireHandler|JobStoreSupport|QuartzScheduler" [5m]))
                seriesNameFormat: quartz
    md_core_dailyreport_not_found_rate:
      kind: Panel
      spec:
        display:
          name: md-core "dailyReport SOLICITADO no encontrado" (req/s)
        plugin:
          kind: TimeSeriesChart
          spec: {}
        queries:
        - kind: TimeSeriesQuery
          spec:
            plugin:
              kind: PrometheusTimeSeriesQuery
              spec:
                datasource:
                  kind: PrometheusDatasource
                  name: loki-pre
                query: |-
                  sum(rate({cluster="pre", container="md-core"}
                           |~ "dailyReport SOLICITADO|PeriodicNotificationGenerator" [5m]))
                seriesNameFormat: dailyReport-not-found
    md_core_intake_reminder_error_rate:
      kind: Panel
      spec:
        display:
          name: md-core IntakeReminderNotificationJob errors (req/s)
        plugin:
          kind: TimeSeriesChart
          spec:
            thresholds:
              steps:
              - value: 0.01
                color: yellow
              - value: 0.05
                color: red
        queries:
        - kind: TimeSeriesQuery
          spec:
            plugin:
              kind: PrometheusTimeSeriesQuery
              spec:
                datasource:
                  kind: PrometheusDatasource
                  name: loki-pre
                query: |-
                  sum(rate({cluster="pre", container="md-core"}
                           |~ "IntakeReminderNotificationJob" [5m]))
                seriesNameFormat: intake-reminder
    md_core_other_error_rate:
      kind: Panel
      spec:
        display:
          name: md-core errors NO catalogados (req/s)
        plugin:
          kind: TimeSeriesChart
          spec:
            thresholds:
              steps:
              - value: 0.02
                color: yellow
              - value: 0.1
                color: red
        queries:
        - kind: TimeSeriesQuery
          spec:
            plugin:
              kind: PrometheusTimeSeriesQuery
              spec:
                datasource:
                  kind: PrometheusDatasource
                  name: loki-pre
                query: |-
                  sum(rate({cluster="pre", container="md-core"}
                           |~ "(?i)error|exception"
                           !~ "JobStoreCMT|ClusterManager|qrtz_|MisfireHandler|JobStoreSupport|QuartzScheduler|dailyReport SOLICITADO|PeriodicNotificationGenerator|IntakeReminderNotificationJob"
                           [5m]))
                seriesNameFormat: other
    md_core_request_failed_rate:
      kind: Panel
      spec:
        display:
          name: md-core "Request failed" (req/s)
        plugin:
          kind: TimeSeriesChart
          spec:
            thresholds:
              steps:
              - value: 0.01
                color: yellow
              - value: 0.05
                color: red
        queries:
        - kind: TimeSeriesQuery
          spec:
            plugin:
              kind: PrometheusTimeSeriesQuery
              spec:
                datasource:
                  kind: PrometheusDatasource
                  name: loki-pre
                query: |-
                  sum(rate({cluster="pre", container="md-core"}
                           |= "Request failed" [5m]))
                seriesNameFormat: md-core
    md_core_no_programming_rate:
      kind: Panel
      spec:
        display:
          name: md-core "No programming found for device" (req/s)
        plugin:
          kind: TimeSeriesChart
          spec: {}
        queries:
        - kind: TimeSeriesQuery
          spec:
            plugin:
              kind: PrometheusTimeSeriesQuery
              spec:
                datasource:
                  kind: PrometheusDatasource
                  name: loki-pre
                query: |-
                  sum(rate({cluster="pre", container="md-core"}
                           |= "No programming found for device" [5m]))
                seriesNameFormat: md-core
    log_volume_by_container:
      kind: Panel
      spec:
        display:
          name: Volumen de log por contenedor (líneas/s)
        plugin:
          kind: TimeSeriesChart
          spec: {}
        queries:
        - kind: TimeSeriesQuery
          spec:
            plugin:
              kind: PrometheusTimeSeriesQuery
              spec:
                datasource:
                  kind: PrometheusDatasource
                  name: loki-pre
                query: |-
                  sum by (container) (
                    rate({cluster="pre"} [5m])
                  )
                seriesNameFormat: '{{container}}'
  layouts:
  - kind: Grid
    spec:
      display:
        title: Main
      items:
      - x: 0
        y: 0
        width: 12
        height: 8
        content:
          $ref: '#/spec/panels/actualizacion_timeout_rate'
      - x: 12
        y: 0
        width: 12
        height: 8
        content:
          $ref: '#/spec/panels/ingress_status_rate'
      - x: 0
        y: 8
        width: 12
        height: 8
        content:
          $ref: '#/spec/panels/actualizacion_failure_ratio'
      - x: 12
        y: 8
        width: 12
        height: 8
        content:
          $ref: '#/spec/panels/md_core_quartz_error_rate'
      - x: 0
        y: 16
        width: 12
        height: 8
        content:
          $ref: '#/spec/panels/md_core_dailyreport_not_found_rate'
      - x: 12
        y: 16
        width: 12
        height: 8
        content:
          $ref: '#/spec/panels/md_core_intake_reminder_error_rate'
      - x: 0
        y: 24
        width: 12
        height: 8
        content:
          $ref: '#/spec/panels/md_core_other_error_rate'
      - x: 12
        y: 24
        width: 12
        height: 8
        content:
          $ref: '#/spec/panels/md_core_request_failed_rate'
      - x: 0
        y: 32
        width: 12
        height: 8
        content:
          $ref: '#/spec/panels/md_core_no_programming_rate'
      - x: 12
        y: 32
        width: 12
        height: 8
        content:
          $ref: '#/spec/panels/log_volume_by_container'
